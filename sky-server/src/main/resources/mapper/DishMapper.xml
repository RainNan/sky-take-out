<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!--在 命名空间 进行映射 com.sky.mapper.DishMapper-->
<mapper namespace="com.sky.mapper.DishMapper">
    <!--    insert 语句完成后，产生的主键值，会赋给 id，这个 id 是 dish 的 id-->
    <insert id="add" useGeneratedKeys="true" keyProperty="id">
        insert into sky_take_out.dish
        (name, category_id, price, image, description, create_time, update_time, create_user, update_user, status)
        values (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{createTime}, #{updateTime}, #{createUser},
                #{updateUser}, #{status})
    </insert>
</mapper>
